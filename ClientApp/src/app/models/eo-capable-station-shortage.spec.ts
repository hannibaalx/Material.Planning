import { EoCapableStationShortage } from './eo-capable-station-shortage';

describe('EoCapableStationShortage', () => {
  it('should create an instance', () => {
    expect(new EoCapableStationShortage()).toBeTruthy();
  });
});
