import { EoOnHoldEosDetail } from './eo-on-hold-eos-detail';

describe('EoOnHoldEosDetail', () => {
  it('should create an instance', () => {
    expect(new EoOnHoldEosDetail()).toBeTruthy();
  });
});
