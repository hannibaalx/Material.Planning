import { ScheduledRotRepShortageDetail } from './scheduled-rot-rep-shortage-detail';

describe('ScheduledRotRepShortageDetail', () => {
  it('should create an instance', () => {
    expect(new ScheduledRotRepShortageDetail()).toBeTruthy();
  });
});
