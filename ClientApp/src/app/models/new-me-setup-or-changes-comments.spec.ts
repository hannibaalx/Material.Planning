import { NewMeSetupOrChangesComments } from './new-me-setup-or-changes-comments';

describe('NewMeSetupOrChangesComments', () => {
  it('should create an instance', () => {
    expect(new NewMeSetupOrChangesComments()).toBeTruthy();
  });
});
