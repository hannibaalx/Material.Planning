import { BaseStaShortDetail } from './base-sta-short-detail';

describe('BaseStaShortDetail', () => {
  it('should create an instance', () => {
    expect(new BaseStaShortDetail()).toBeTruthy();
  });
});
