import { ReviewReasonNotification } from './review-reason-notification';

describe('ReviewReasonNotification', () => {
  it('should create an instance', () => {
    expect(new ReviewReasonNotification()).toBeTruthy();
  });
});
