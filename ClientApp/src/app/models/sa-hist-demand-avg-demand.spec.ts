import { SaHistDemandAvgDemand } from './sa-hist-demand-avg-demand';

describe('SaHistDemandAvgDemand', () => {
  it('should create an instance', () => {
    expect(new SaHistDemandAvgDemand()).toBeTruthy();
  });
});
