import { SoPendingReviewDetail } from './so-pending-review-detail';

describe('SoPendingReviewDetail', () => {
  it('should create an instance', () => {
    expect(new SoPendingReviewDetail()).toBeTruthy();
  });
});
