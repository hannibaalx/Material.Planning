import { AircraftAccomplished } from './aircraft-accomplished';

describe('AircraftAccomplished', () => {
  it('should create an instance', () => {
    expect(new AircraftAccomplished()).toBeTruthy();
  });
});
