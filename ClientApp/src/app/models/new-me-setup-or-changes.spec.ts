import { NewMeSetupOrChanges } from './new-me-setup-or-changes';

describe('NewMeSetupOrChanges', () => {
  it('should create an instance', () => {
    expect(new NewMeSetupOrChanges()).toBeTruthy();
  });
});
