import { OpenDiscrepancyAlert } from './open-discrepancy-alert';

describe('OpenDiscrepancyAlert', () => {
  it('should create an instance', () => {
    expect(new OpenDiscrepancyAlert()).toBeTruthy();
  });
});
