import { SmScheduledRotableBomComments } from './sm-scheduled-rotable-bom-comments';

describe('SmScheduledRotableBomComments', () => {
  it('should create an instance', () => {
    expect(new SmScheduledRotableBomComments()).toBeTruthy();
  });
});
