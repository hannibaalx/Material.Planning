import { SmScheduledNextDueDateDetail } from './sm-scheduled-next-due-date-detail';

describe('SmScheduledNextDueDateDetail', () => {
  it('should create an instance', () => {
    expect(new SmScheduledNextDueDateDetail()).toBeTruthy();
  });
});
