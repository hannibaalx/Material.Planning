import { ScheduledRotRepShortageComment } from './scheduled-rot-rep-shortage-comment';

describe('ScheduledRotRepShortageComment', () => {
  it('should create an instance', () => {
    expect(new ScheduledRotRepShortageComment()).toBeTruthy();
  });
});
