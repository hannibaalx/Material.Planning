import { SmPartNoLongerUsedComments } from './sm-part-no-longer-used-comments';

describe('SmPartNoLongerUsedComments', () => {
  it('should create an instance', () => {
    expect(new SmPartNoLongerUsedComments()).toBeTruthy();
  });
});
