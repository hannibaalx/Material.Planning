import { CatalogExpirationAlert } from './catalog-expiration-alert';

describe('CatalogExpirationComment', () => {
  it('should create an instance', () => {
    expect(new CatalogExpirationAlert()).toBeTruthy();
  });
});
