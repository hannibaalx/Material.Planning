import { OwnershipNoAllocationAlert } from './ownership-no-allocation-alert';

describe('OwnershipNoAllocationAlert', () => {
  it('should create an instance', () => {
    expect(new OwnershipNoAllocationAlert()).toBeTruthy();
  });
});
