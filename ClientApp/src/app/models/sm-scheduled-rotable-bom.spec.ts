import { SmScheduledRotableBom } from './sm-scheduled-rotable-bom';

describe('SmScheduledRotableBom', () => {
  it('should create an instance', () => {
    expect(new SmScheduledRotableBom()).toBeTruthy();
  });
});
