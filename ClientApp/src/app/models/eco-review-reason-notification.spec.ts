import { EcoReviewReasonNotification } from './eco-review-reason-notification';

describe('EcoReviewReasonNotification', () => {
  it('should create an instance', () => {
    expect(new EcoReviewReasonNotification()).toBeTruthy();
  });
});
