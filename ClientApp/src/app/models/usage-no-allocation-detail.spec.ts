import { UsageNoAllocationDetail } from './usage-no-allocation-detail';

describe('UsageNoAllocationDetail', () => {
  it('should create an instance', () => {
    expect(new UsageNoAllocationDetail()).toBeTruthy();
  });
});
