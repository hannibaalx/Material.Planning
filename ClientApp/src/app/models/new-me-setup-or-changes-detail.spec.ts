import { NewMeSetupOrChangesDetail } from './new-me-setup-or-changes-detail';

describe('NewMeSetupOrChangesDetail', () => {
  it('should create an instance', () => {
    expect(new NewMeSetupOrChangesDetail()).toBeTruthy();
  });
});
