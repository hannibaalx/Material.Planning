import { TesCriticalComments } from './tes-critical-comments';

describe('TesCriticalComments', () => {
  it('should create an instance', () => {
    expect(new TesCriticalComments()).toBeTruthy();
  });
});
