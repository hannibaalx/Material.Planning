import { MainStationBalancingDetail } from './main-station-balancing-detail';

describe('MainStationBalancingDetail', () => {
  it('should create an instance', () => {
    expect(new MainStationBalancingDetail()).toBeTruthy();
  });
});
