import { MainStationBalancing } from './main-station-balancing';

describe('MainStationBalancing', () => {
  it('should create an instance', () => {
    expect(new MainStationBalancing()).toBeTruthy();
  });
});
