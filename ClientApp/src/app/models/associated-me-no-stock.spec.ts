import { AssociatedMeNoStock } from './associated-me-no-stock';

describe('AssociatedMeNoStock', () => {
  it('should create an instance', () => {
    expect(new AssociatedMeNoStock()).toBeTruthy();
  });
});
