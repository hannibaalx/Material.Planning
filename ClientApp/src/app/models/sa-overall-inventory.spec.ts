import { SaOverallInventory } from './sa-overall-inventory';

describe('SaOverallInventory', () => {
  it('should create an instance', () => {
    expect(new SaOverallInventory()).toBeTruthy();
  });
});
