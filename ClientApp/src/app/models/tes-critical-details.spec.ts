import { TesCriticalDetails } from './tes-critical-details';

describe('TesCriticalDetails', () => {
  it('should create an instance', () => {
    expect(new TesCriticalDetails()).toBeTruthy();
  });
});
