import { OpenDiscrepancyDetail } from './open-discrepancy-detail';

describe('OpenDiscrepancyDetail', () => {
  it('should create an instance', () => {
    expect(new OpenDiscrepancyDetail()).toBeTruthy();
  });
});
