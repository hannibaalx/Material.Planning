import { VendorWoAssignment } from './vendor-wo-assignment';

describe('VendorWoAssignment', () => {
  it('should create an instance', () => {
    expect(new VendorWoAssignment()).toBeTruthy();
  });
});
