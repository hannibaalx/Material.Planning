import { EoAdPartsShortage } from './eo-ad-parts-shortage';

describe('EoAdPartsShortage', () => {
  it('should create an instance', () => {
    expect(new EoAdPartsShortage()).toBeTruthy();
  });
});
