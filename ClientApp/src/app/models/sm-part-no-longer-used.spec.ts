import { SmPartNoLongerUsed } from './sm-part-no-longer-used';

describe('SmPartNoLongerUsed', () => {
  it('should create an instance', () => {
    expect(new SmPartNoLongerUsed()).toBeTruthy();
  });
});
