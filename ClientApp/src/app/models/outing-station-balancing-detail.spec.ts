import { OutingStationBalancingDetail } from './outing-station-balancing-detail';

describe('OutingStationBalancingDetail', () => {
  it('should create an instance', () => {
    expect(new OutingStationBalancingDetail()).toBeTruthy();
  });
});
