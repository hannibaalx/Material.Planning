import { UsageNoAllocation } from './usage-no-allocation';

describe('UsageNoAllocation', () => {
  it('should create an instance', () => {
    expect(new UsageNoAllocation()).toBeTruthy();
  });
});
