import { EoNewStationsChanges } from './eo-new-stations-changes';

describe('EoNewStationsChanges', () => {
  it('should create an instance', () => {
    expect(new EoNewStationsChanges()).toBeTruthy();
  });
});
