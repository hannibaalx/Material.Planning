import { SmBaselineStationShortageComment } from './sm-baseline-station-shortage-comment';

describe('SmBaselineStationShortageComment', () => {
  it('should create an instance', () => {
    expect(new SmBaselineStationShortageComment()).toBeTruthy();
  });
});
