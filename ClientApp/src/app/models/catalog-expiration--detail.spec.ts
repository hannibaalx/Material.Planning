import { CatalogExpirationDetail } from './catalog-expiration--detail';

describe('CatalogExpirationDetail', () => {
  it('should create an instance', () => {
    expect(new CatalogExpirationDetail()).toBeTruthy();
  });
});
