import { SmNewStationsChangesComments } from './sm-new-stations-changes-comments';

describe('SmNewStationsChangesComments', () => {
  it('should create an instance', () => {
    expect(new SmNewStationsChangesComments()).toBeTruthy();
  });
});
