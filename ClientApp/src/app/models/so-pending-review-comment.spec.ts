import { SoPendingReviewComment } from './so-pending-review-comment';

describe('SoPendingReviewComment', () => {
  it('should create an instance', () => {
    expect(new SoPendingReviewComment()).toBeTruthy();
  });
});
