import { MeSummarySearchType } from './me-summary-search-type';

describe('MeSummarySearchType', () => {
  it('should create an instance', () => {
    expect(new MeSummarySearchType()).toBeTruthy();
  });
});
