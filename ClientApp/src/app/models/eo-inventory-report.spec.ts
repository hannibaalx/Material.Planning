import { EoInventoryReport } from './eo-inventory-report';

describe('EoInventoryReport', () => {
  it('should create an instance', () => {
    expect(new EoInventoryReport()).toBeTruthy();
  });
});
