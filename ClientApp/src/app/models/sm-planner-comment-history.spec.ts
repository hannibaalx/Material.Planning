import { SmPlannerCommentHistory } from './sm-planner-comment-history';

describe('SmPlannerCommentHistory', () => {
  it('should create an instance', () => {
    expect(new SmPlannerCommentHistory()).toBeTruthy();
  });
});
