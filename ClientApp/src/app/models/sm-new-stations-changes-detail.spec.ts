import { SmNewStationsChangesDetail } from './sm-new-stations-changes-detail';

describe('SmNewStationsChangesDetail', () => {
  it('should create an instance', () => {
    expect(new SmNewStationsChangesDetail()).toBeTruthy();
  });
});
