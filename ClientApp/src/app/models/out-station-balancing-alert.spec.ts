import { OutStationBalancingAlert } from './out-station-balancing-alert';

describe('OutStationBalancingAlert', () => {
  it('should create an instance', () => {
    expect(new OutStationBalancingAlert()).toBeTruthy();
  });
});
