import { EoPartsShortage30 } from './eo-parts-shortage30';

describe('EoPartsShortage30', () => {
  it('should create an instance', () => {
    expect(new EoPartsShortage30()).toBeTruthy();
  });
});
