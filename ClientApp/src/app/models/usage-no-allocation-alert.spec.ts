import { UsageNoAllocationAlert } from './usage-no-allocation-alert';

describe('UsageNoAllocationAlert', () => {
  it('should create an instance', () => {
    expect(new UsageNoAllocationAlert()).toBeTruthy();
  });
});
