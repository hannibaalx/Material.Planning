import { OwnerNoAllocationDetail } from './owner-no-allocation-detail';

describe('OwnerNoAllocationDetail', () => {
  it('should create an instance', () => {
    expect(new OwnerNoAllocationDetail()).toBeTruthy();
  });
});
